require 'rails_helper'
  describe Apply,type: :model do
    
      
      it "is invalid without an event_id" do
         
      end
      
      
       it "is invalid without an student_id" do
         
      end
      
       it "is valid with student_id event_id" do
        # student = create(:student,:with_apply)
        # event=create()
        # student.valid?
        # expect(student.apply).to be_valid
      end
      
  end